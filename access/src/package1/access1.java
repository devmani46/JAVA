package package1;

public class access1 {
	
	//default, public,private and protected
	//default and public can transfer in different classes within different packages
	// private can only do in one class
	// protected can transfer in different classes but not in different packages
	protected int hours = 2;
	protected int minutes = 46;
	
}
